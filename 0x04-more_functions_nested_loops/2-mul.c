#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: first parameter
 * @b: second parameter
 * Return: multiplies two integers
 */
int mul(int a, int b)
{
int i;
i = a * b;
return (i);
}
