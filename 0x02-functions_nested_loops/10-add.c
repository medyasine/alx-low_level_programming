#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: first number
 * @b: second number
 * Return: 0 (Success)
 */
int add(int a, int b)
{
int c;
c = a + b;
return (c);
}
