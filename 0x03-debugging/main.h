#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
ent largest_number(int a, int b, int c);
int convert_day(int month, int day);
ons_nested_loopsid print_remaining_days(int month, int day, int year);

#endif /* MAIN_H */
